{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Data Validator (Flask)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "console": "integratedTerminal",
      "justMyCode": true,
      "env": {
        "FLASK_ENV": "development",
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
